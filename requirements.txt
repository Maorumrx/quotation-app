# ---- runtime ----
fastapi>=0.110
uvicorn>=0.29
pywebview>=5.0
pydantic>=2.7
xhtml2pdf>=0.2.17   # สร้างไฟล์ PDF ฝั่ง server (เพียว Python + ดึง reportlab มาเอง ไม่มี native dep)

# ---- build only (CI ลงให้เอง) ----
# pyinstaller
