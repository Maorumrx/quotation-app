# ---- runtime ----
fastapi>=0.110
uvicorn>=0.29
pywebview>=5.0
pydantic>=2.7
certifi>=2024.2     # ใบรับรอง CA สำหรับยิง HTTPS ไป GitHub (เช็คอัปเดต) — ตอน frozen build หา CA เองไม่เจอ
# PDF: ทำฝั่งเว็บวิว (window.print -> Save as PDF) ให้เหมือนหน้าจอ — ไม่ต้องพึ่ง xhtml2pdf/reportlab แล้ว

# ---- build only (CI ลงให้เอง) ----
# pyinstaller
